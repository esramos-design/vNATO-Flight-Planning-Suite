vNATO Flight Planning Suite V5.7.7 — TEAM LIVE

Runtime-only public build.
- View Page Source exposes only a minimal bootstrap document.
- Application markup is encoded inside js/app-shell.obf.js.
- JavaScript payloads are encoded/obfuscated wrappers.
- CSS is minified and comments are removed.
- No source maps, tests, docs, changelogs or readable master source are shipped.

IMPORTANT: This is source-code deterrence, not cryptographic protection. Any HTML/CSS/JavaScript delivered to a browser can ultimately be inspected, decoded or reconstructed by a sufficiently determined user. Sensitive secrets must never be placed in client-side code.
